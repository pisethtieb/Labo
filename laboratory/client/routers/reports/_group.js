LaboratoryRoutes = FlowRouter.group({
    prefix: '/laboratory'
    //subscriptions: function (params, queryParams) {
    //    // Global subscriptions for route group
    //    this.register('laboratory_address', Meteor.subscribe('laboratory_address'));
    //}
});
